let RemakeStore;_59f‍.x([["getDirForUserData",()=>getDirForUserData],["getDirForBootstrapDataFile",()=>getDirForBootstrapDataFile],["getDirForGlobalData",()=>getDirForGlobalData],["getDirForRemakeAppData",()=>getDirForRemakeAppData],["getDirForUpload",()=>getDirForUpload],["getDirForPageTemplate",()=>getDirForPageTemplate],["getDirForLayoutTemplate",()=>getDirForLayoutTemplate],["getDirForAllPartials",()=>getDirForAllPartials],["getDirForPartialTemplate",()=>getDirForPartialTemplate],["getDirForRootApp",()=>getDirForRootApp]]);_59f‍.w("../lib/remake-store",[["default",["RemakeStore"],function(v){RemakeStore=v}]]);const path = require("upath");


       function getDirForUserData({ appName, withFile, username }) {
  if (withFile && !username) {
    console.log(
      "Error: getDirForUserData() requires a 'username' if you pass in the 'withFile' argument"
    );
    return;
  }

  let innerAppPath = RemakeStore.isMultiTenant() ? appName + "/" : "";
  let commonPath = `../../app/${innerAppPath}data/database`;
  let filePath = withFile ? username + ".json" : "";
  return [
    path.join(__dirname, `${commonPath}/user-app-data/${filePath}`),
    path.join(__dirname, `${commonPath}/user-details/${filePath}`),
  ];
}

       function getDirForBootstrapDataFile({ fileName, appName }) {
  let innerAppPath = RemakeStore.isMultiTenant() ? appName + "/" : "";
  let pathString = `../../app/${innerAppPath}data/${fileName}.json`;
  return path.join(__dirname, pathString);
}

       function getDirForGlobalData({ appName }) {
  let innerAppPath = RemakeStore.isMultiTenant() ? appName + "/" : "";
  let pathString = `../../app/${innerAppPath}data/global.json`;
  return path.join(__dirname, pathString);
}

       function getDirForRemakeAppData({ appName }) {
  let innerAppPath = RemakeStore.isMultiTenant() ? appName + "/" : "";
  let pathString = `../../app/${innerAppPath}data/_remake-app-data.json`;
  return path.join(__dirname, pathString);
}

       function getDirForUpload({ appName, username }) {
  let innerAppPath = RemakeStore.isMultiTenant() ? appName + "/" : "";
  let pathString = `../../app/${innerAppPath}data/uploads/${username}`;
  return path.join(__dirname, pathString);
}

let pageNamesForUserRoutes = ["login", "signup", "reset", "forgot"];
       function getDirForPageTemplate({ pageName, appName }) {
  let innerAppPath = RemakeStore.isMultiTenant() ? appName + "/" : "";
  let innerPagePath = pageNamesForUserRoutes.includes(pageName) ? "user/" : "";
  let pathString = `../../app/${innerAppPath}pages/${innerPagePath}${pageName}.hbs`;
  return path.join(__dirname, pathString);
}

       function getDirForLayoutTemplate({ appName, layoutName }) {
  let innerAppPath = RemakeStore.isMultiTenant() ? appName + "/" : "";
  let pathString = `../../app/${innerAppPath}layouts/${layoutName}.hbs`;
  return path.join(__dirname, pathString);
}

       function getDirForAllPartials({ appName }) {
  let innerAppPath = RemakeStore.isMultiTenant() ? appName + "/" : "";
  let pathString = `../../app/${innerAppPath}partials`;
  return path.join(__dirname, pathString);
}

       function getDirForPartialTemplate({ appName, partialName }) {
  let innerAppPath = RemakeStore.isMultiTenant() ? appName + "/" : "";
  let pathString = `../../app/${innerAppPath}partials/${partialName}.hbs`;
  return path.join(__dirname, pathString);
}

       function getDirForRootApp() {
  let pathString = `../../app`;
  return path.join(__dirname, pathString);
}
