let lodash,deepdash;_e4b‍.x([["getItemWithId",()=>getItemWithId]]);_e4b‍.w("lodash",[["default",["lodash"],function(v){lodash=v}]]);_e4b‍.w("deepdash",[["default",["deepdash"],function(v){deepdash=v}]]);


const _ = deepdash(lodash);

       function getItemWithId(data, id) {
  let currentItem;

  _.forEachDeep(data, function (value, key, parentValue, context) {
    // get the data for the id in the route param if there is one
    if (_.isPlainObject(value) && value.id && value.id === id) {
      currentItem = value;
    }
  });

  return currentItem;
}
